<section class="studio-section v8-template" aria-labelledby="v8-template-title">
  <div class="studio-shell v8-template-grid">
    <div>
      <p class="studio-kicker">Free practical tool · no signup required</p>
      <h2 id="v8-template-title" class="studio-display studio-section-title">Test an AI workflow<br />
        before you <strong>trust it</strong><span class="text-signal">.</span></h2>
      <p class="studio-section-lede">Run ten normal, broken, restricted, duplicate, and recovery cases. Record what happened and decide whether the process is ready for real work.</p>
      <div class="v8-template-actions">
        <a href="/workflow-testing-template/" class="studio-button studio-button--primary">
          Use the free 10-case test
          <svg viewBox="0 0 24 24" aria-hidden="true"><path d="M5 12h13M13 6l6 6-6 6" /></svg>
        </a>
        <a href="/workflow-book/" class="studio-button studio-button--secondary">Get all companion templates</a>
      </div>
    </div>

    <div class="v8-template-map" aria-hidden="true">
      {Array.from({ length: 10 }, (_, index) => (
        <span class:list={{ "is-failure": [3, 5, 6, 7, 8, 9].includes(index) }}>{String(index + 1).padStart(2, "0")}</span>
      ))}
      <b>RUN</b><i></i><b>STOP</b><i></i><b>RECOVER</b>
    </div>
  </div>
</section>

<style>
  .v8-template {
    background:
      radial-gradient(circle at 78% 48%, rgb(40 81 255 / 0.17), transparent 22rem),
      #090b10;
  }

  .v8-template-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.08fr) minmax(340px, 0.72fr);
    gap: clamp(3rem, 8vw, 8rem);
    align-items: center;
  }

  .v8-template .studio-section-lede {
    margin-top: 1.75rem;
  }

  .v8-template-actions {
    display: flex;
    flex-wrap: wrap;
    gap: 0.9rem;
    margin-top: 2rem;
  }

  .v8-template-map {
    display: grid;
    grid-template-columns: repeat(5, minmax(0, 1fr));
    gap: 0.7rem;
    padding: clamp(1.3rem, 3vw, 2.4rem);
    border: 1px solid rgb(40 81 255 / 0.48);
    background:
      linear-gradient(rgb(255 255 255 / 0.025) 1px, transparent 1px),
      linear-gradient(90deg, rgb(255 255 255 / 0.02) 1px, transparent 1px),
      rgb(5 6 8 / 0.82);
    background-size: 42px 42px;
  }

  .v8-template-map span {
    display: grid;
    aspect-ratio: 1;
    place-items: center;
    border: 1px solid rgb(40 81 255 / 0.62);
    background: rgb(40 81 255 / 0.08);
    color: var(--color-cobalt-text);
    font-family: var(--font-mono);
    font-size: 0.66rem;
  }

  .v8-template-map span.is-failure {
    border-color: rgb(255 96 77 / 0.5);
    background: rgb(255 96 77 / 0.06);
    color: var(--color-signal);
  }

  .v8-template-map b {
    margin-top: 1rem;
    color: var(--color-paper-dim);
    font-family: var(--font-mono);
    font-size: 0.65rem;
    letter-spacing: 0.08em;
    text-align: center;
  }

  .v8-template-map i {
    align-self: center;
    height: 1px;
    margin-top: 1rem;
    background: linear-gradient(90deg, var(--color-cobalt), var(--color-signal));
  }

  @media (max-width: 850px) {
    .v8-template-grid {
      grid-template-columns: minmax(0, 1fr);
    }

    .v8-template-map {
      width: min(100%, 560px);
    }
  }

  @media (max-width: 520px) {
    .v8-template-actions {
      display: grid;
      grid-template-columns: 1fr;
    }

    .v8-template-actions .studio-button {
      width: 100%;
    }
  }
</style>
