---
import { site } from "../data/site";
const year = new Date().getFullYear();
---

<footer class="studio-footer">
  <div class="studio-shell">
    <a href="/" class="studio-footer-name">Keith Staggers<span>.</span></a>
    <div class="studio-footer-grid">
      <div>
        <p>AI creative studio</p>
        <p>Available worldwide</p>
      </div>
      <nav aria-label="Footer navigation">
        {site.nav.map((item) => <a href={item.href}>{item.label}</a>)}
      </nav>
      <nav aria-label="Social links">
        <a href={site.social.facebook} target="_blank" rel="noreferrer">Facebook</a>
        <a href={site.social.instagram} target="_blank" rel="noreferrer">Instagram</a>
        <a href={site.social.spotify} target="_blank" rel="noreferrer">Spotify</a>
      </nav>
      <div>
        <a href={`mailto:${site.email}`}>{site.email}</a>
        <p>© {year}</p>
      </div>
    </div>
  </div>
</footer>
